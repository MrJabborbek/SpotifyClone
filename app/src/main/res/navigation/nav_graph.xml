<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nev_graph"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.fraggeil.spotifyclone.ui.MainActivity"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" />
    <fragment
        android:id="@+id/songFragment"
        android:name="com.fraggeil.spotifyclone.ui.fragmens.SongFragment"
        android:label="SongFragment" />
    <action
        android:id="@+id/action_global_songFragment"
        app:destination="@id/songFragment" />
</navigation>